@page
@using ContosoCrafts.WebSite.Components
@using ContosoCrafts.WebSite.Services
@inject JsonFileArticleService ArticleService
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}
<script src="_framework/blazor.server.js"></script>
<h3>The biscuits may be undercooked, but the articles are spot on.</h3>

<!--
<style>
    h3{
    font-family: "Brush Script MT ",Lucida Handwriting,Cursive;
        font-size: 30px;
    }
        body{
        background-color: peachpuff;
    }
    }
</style>
-->

<h3>Articles worth reading!</h3>

<div id="mycarousel" class="carousel slide" data-ride="carousel">
    <div class="carousel-inner">
        @{
            int i = 0;
            foreach (var article in ArticleService.GetAllData())
            {
                string classText = (i == 0) ? "active" : "";
                    <div class="carousel-item + @classText">
                        <div class="d-flex justify-content-center w-100 h-100">
                            <img src="@article.Image" height="600" margin=auto>
                            <div class="carousel-caption d-none d-md-block">
                                <h5>@article.Title</h5>
                                <p>@article.Description</p>
                            </div>
                        </div>
                    </div>
                i++;
            }
        }
    </div>
</div>

<a class="carousel-control-prev" href="#mycarousel" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
</a>

<a class="carousel-control-next" href="#mycarousel" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
</a>